package legged_hil_lcm;

struct joints_cmd_t
{
  int32_t  num_joints;

  int32_t  sec[num_joints];
  int32_t  nsec[num_joints];  

  string name[num_joints];

  double pos_des[num_joints];
  double vel_des[num_joints];
  double kp[num_joints];
  double kd[num_joints];
  double ff[num_joints];
}